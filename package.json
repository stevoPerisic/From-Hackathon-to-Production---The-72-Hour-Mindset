{
  "name": "from-hackathon-to-production",
  "private": true,
  "type": "module",
  "scripts": {
    "lint:md": "markdownlint \"**/*.md\" --config .markdownlint.jsonc --ignore node_modules --ignore exports",
    "new:chapter": "node scripts/new-chapter.mjs",
    "build": "node scripts/build.mjs"
  },
  "devDependencies": {
    "markdownlint-cli": "^0.41.0"
  }
}
